<!--<Window x:Class="PL.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="Login" Height="450" Width="400"
        xmlns:helpers="clr-namespace:PL"
         Icon="icon.ico"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Grid>
        <Label Content="Welcome Back" HorizontalAlignment="Center" Margin="0,41,0,0" VerticalAlignment="Top" Width="196" Height="48" FontSize="29"/>
        <Label Content="Identity No." HorizontalAlignment="Left" Margin="102,106,0,0" VerticalAlignment="Top" Width="80"/>
        <TextBox HorizontalAlignment="Center" Text="{Binding Id, Mode=OneWayToSource}" Margin="0,132,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="196" Height="35" TextChanged="TextBoxId_TextChanged"/>
        <Label Content="Password" HorizontalAlignment="Left" Margin="102,184,0,0" VerticalAlignment="Top" Width="80"/>
        <PasswordBox Width="196" Height="35" Margin="102,215,102,184" helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay}" />
        <Button Content="Login as Admin" HorizontalAlignment="Center" Margin="0,287,0,0" VerticalAlignment="Top" Width="196" Height="35" Click="ButtonLoginAsAdmin_Click"/>
        <Button Content="Login as Volunteer" HorizontalAlignment="Center" Margin="0,337,0,0" VerticalAlignment="Top" Width="196" Height="35" Click="ButtonLoginAsVolunteer_Click"/>
    </Grid>
</Window>-->
<Window x:Class="PL.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        xmlns:helpers="clr-namespace:PL"
        mc:Ignorable="d"
        Title="We Save Lives - Login" Height="612" Width="450"
        Icon="icon.ico"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        >

    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F8F9FA" Offset="0"/>
            <GradientStop Color="#E9ECEF" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Window.Resources>
        <!-- Logo Style -->
        <Style x:Key="LogoStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#DC3545"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

        <!-- Welcome Text Style -->
        <Style x:Key="WelcomeStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,10,0,30"/>
        </Style>

        <!-- Input Label Style -->
        <Style x:Key="InputLabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <!-- Input TextBox Style -->
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#DC3545"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Input PasswordBox Style -->
        <Style x:Key="InputPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#DC3545"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C82333"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#A71E2A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#DC3545"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#DC3545"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DC3545"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="40"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="15" ShadowDepth="5" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Heart Icon Style -->
        <Style x:Key="HeartIconStyle" TargetType="Ellipse">
            <Setter Property="Fill" Value="#DC3545"/>
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
    </Window.Resources>

    <Grid Margin="0,0,0,-6">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Main Login Card -->
        <Border Style="{StaticResource CardStyle}" MaxWidth="450" HorizontalAlignment="Center"  Grid.RowSpan="3" Width="430" Margin="0,10,0,10">
            <StackPanel >
                <!-- Logo Section -->
                <StackPanel HorizontalAlignment="Center" >
                    <!-- Heart Icon Placeholder -->
                    <!--<Ellipse Style="{StaticResource HeartIconStyle}" HorizontalAlignment="Center"/>-->
                    <TextBlock Text="WE SAVE LIVES" Style="{StaticResource LogoStyle}"/>
                </StackPanel>

                <!-- Welcome Message -->
                <TextBlock Text="Welcome Back" Style="{StaticResource WelcomeStyle}"/>

                <!-- Login Form -->
                <StackPanel>
                    <!-- Identity Number Field -->
                    <Label Content="Identity Number" Style="{StaticResource InputLabelStyle}"/>
                    <TextBox Text="{Binding Id, Mode=OneWayToSource}" 
                             Style="{StaticResource InputTextBoxStyle}" 
                             TextChanged="TextBoxId_TextChanged"
                             Margin="0,0,0,20"/>

                    <!-- Password Field -->
                    <Label Content="Password" Style="{StaticResource InputLabelStyle}"/>
                    <PasswordBox helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay}" 
                                 Style="{StaticResource InputPasswordBoxStyle}"
                                 Margin="0,0,0,30"/>

                    <!-- Login Buttons -->
                    <Button Content="Login as Administrator" 
                            Click="ButtonLoginAsAdmin_Click" 
                            Style="{StaticResource PrimaryButtonStyle}"/>

                    <Button Content="Login as Volunteer" 
                            Click="ButtonLoginAsVolunteer_Click" 
                            Style="{StaticResource SecondaryButtonStyle}"/>
                </StackPanel>

                <!-- Footer Text -->
                <TextBlock Text="Emergency Response Management System" 
                           FontSize="12" 
                           Foreground="#6C757D" 
                           HorizontalAlignment="Center" 
                           />
            </StackPanel>
        </Border>
    </Grid>
</Window>
