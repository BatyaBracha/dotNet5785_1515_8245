<!--<Window x:Class="PL.Call.CallListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Call"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        Title="CallListWindow" Height="530" Width="800"
        Icon="../icon.ico"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Background="#F9F9F9" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"/>
            --><!-- For the Internal, Upper Grid --><!--
            <RowDefinition Height="500*"/>
            --><!-- For the DataGrid --><!--
        </Grid.RowDefinitions>

        --><!-- Internal, Upper Grid --><!--
        <Grid Grid.Row="0" VerticalAlignment="Top" Height="72" RenderTransformOrigin="0.499,0.764" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                --><!-- For the Label --><!--
                <ColumnDefinition Width="*"/>
                --><!-- For the ComboBox --><!--
                <ColumnDefinition Width="Auto"/>
                --><!-- For the Add Button --><!--
            </Grid.ColumnDefinitions>

            <Label HorizontalAlignment="Left" VerticalAlignment="Top" Content="Calls Details" 
            FontSize="24" FontWeight="Bold" Foreground="#333333" Margin="78,17,0,0"/>
            --><!--<StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">-->

            <!-- Label for Volunteer Fields ComboBox --><!--
            <Label Content="Sort by Field:" 
            HorizontalAlignment="Left" VerticalAlignment="Top" 
            Margin="74,6,0,0" Grid.Column="1" Foreground="#333333" Width="117"/>

            --><!-- Sorting ComboBox --><!--
            <ComboBox SelectedValue="{Binding Path=CallFieldsFilter, Mode=TwoWay}" 
            ItemsSource="{Binding Source={StaticResource CallFieldsCollectionKey}}" 
            SelectionChanged="comboBoxCallList_SelectionChanged" 
            Width="150" Margin="194,2,46,40" Background="White" BorderBrush="#CCCCCC" Height="30" Grid.Column="1" RenderTransformOrigin="0.747,0.351"/>

            --><!-- Label for Type of Call ComboBox --><!--
            <Label Content="Filter by Call Type:" 
            HorizontalAlignment="Left" VerticalAlignment="Top" 
            Margin="74,44,0,0" Grid.Column="1" Foreground="#333333" Width="115"/>

            <ComboBox SelectedValue="{Binding SelectedTypeOfCallFilter, Mode=TwoWay}" 
                  ItemsSource="{Binding Source={StaticResource TypeOfCallCollectionKey}}" 
                  SelectionChanged="comboBoxCallList_SelectionChanged" 
                  Width="150" Background="White" BorderBrush="#CCCCCC" Height="30" Grid.Column="1" Margin="194,40,46,2"/>
            --><!--</StackPanel>-->

            <!-- Add Button with Icon --><!--
            <Button HorizontalAlignment="Left" VerticalAlignment="Center" Width="50" Height="50" 
            Click="AddButton_Click" Background="#4CAF50" BorderBrush="#388E3C" BorderThickness="1"  Content="➕" Margin="18,0,0,0"/>
        </Grid>
        --><!-- DataGrid --><!--
        <DataGrid Grid.Row="1"
           Margin="0,10,10,0"
           ItemsSource="{Binding Path=CallList}"
           AutoGenerateColumns="False"
           IsReadOnly="True"
           SelectedItem="{Binding SelectedCall}"
           MouseDoubleClick="lsvCallList_MouseDoubleClick"
           Background="White" 
           BorderBrush="#CCCCCC" 
           BorderThickness="1"
           RowBackground="White" 
           AlternatingRowBackground="#F5F5F5"
           HorizontalGridLinesBrush="#E0E0E0"
           VerticalGridLinesBrush="#E0E0E0">

            --><!--    public int? Id {  get; set; }
    public int CallId { get; set; }
    public TypeOfCall TypeOfCall { get; set; }
    public DateTime OpeningTime { get; init; }
    public TimeSpan? TimeLeft { get; set; }
    public string? LastVolunteerName { get; set; }
    public TimeSpan? TreatmentDuration { get; set; }
    public CallStatus Status { get; set; }
    public int AssignmentsSum {  get; set; }--><!--

            <DataGrid.Columns>
                --><!-- Id --><!--
                <DataGridTextColumn Binding="{Binding CallId}" Header="ID" Width="0.3*"/>
                --><!-- Name --><!--
                <DataGridTextColumn Binding="{Binding TypeOfCall}" Header="Type Of Call" Width="0.8*" />

                --><!-- Active (CheckBox) -->
                <!--<DataGridTemplateColumn Header="Active" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding Active}" IsEnabled="False" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>-->

                <!-- CallsOutOfDate --><!--
                <DataGridTextColumn Binding="{Binding OpeningTime}" Header="Opening Time" Width="0.9*" />

                --><!-- CallsCanceled --><!--
                <DataGridTextColumn Binding="{Binding TimeLeft}" Header="Time Left" Width="0.6*" />

                --><!-- CallsDone --><!--
                <DataGridTextColumn Binding="{Binding LastVolunteerName}" Header="Last Volunteer Name" Width="*" />

                <DataGridTextColumn Binding="{Binding TreatmentDuration}" Header="Treatment Duration" Width="1.2*" />

                <DataGridTextColumn Binding="{Binding Status}" Header="Status" Width="0.5*" />

                <DataGridTextColumn Binding="{Binding AssignmentsSum}" Header="Assignments Sum" Width="*" />



                --><!-- Delete button with Icon --><!--
                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Click="DeleteButton_Click" Tag="{Binding CallId}" Content="❌"
                        Background="#F44336" BorderBrush="#D32F2F" BorderThickness="1" Width="30" Height="30">
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>-->
<Window x:Class="PL.Call.CallListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Call"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        Title="We Save Lives - Calls" Height="700" Width="1000"
        Icon="../icon.ico"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        WindowStartupLocation="CenterScreen">

    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F8F9FA" Offset="0"/>
            <GradientStop Color="#E9ECEF" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Window.Resources>
        <!-- Header Style -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#DC3545"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,10,0,20"/>
        </Style>

        <!-- Filter Label Style -->
        <Style x:Key="FilterLabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <!-- Filter ComboBox Style -->
        <Style x:Key="FilterComboBoxStyle" TargetType="ComboBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton" 
                                              BorderBrush="Transparent" 
                                              Background="Transparent" 
                                              Grid.Column="2"
                                              Focusable="false"
                                              IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="Transparent">
                                                <Path x:Name="Arrow" 
                                                      Data="M 0 0 L 4 4 L 8 0 Z" 
                                                      Fill="#495057" 
                                                      HorizontalAlignment="Right" 
                                                      VerticalAlignment="Center" 
                                                      Margin="0,0,10,0"/>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter Name="ContentSite" 
                                                  IsHitTestVisible="False" 
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
                                <TextBox x:Name="PART_EditableTextBox" 
                                         Style="{x:Null}"
                                         Background="Transparent" 
                                         BorderThickness="0" 
                                         Focusable="True" 
                                         Visibility="Hidden"
                                         IsReadOnly="{TemplateBinding IsReadOnly}"/>
                                <Popup Name="Popup" 
                                       Placement="Bottom" 
                                       IsOpen="{TemplateBinding IsDropDownOpen}" 
                                       AllowsTransparency="True" 
                                       Focusable="False" 
                                       PopupAnimation="Slide">
                                    <Grid Name="DropDown" 
                                          SnapsToDevicePixels="True" 
                                          MinWidth="{TemplateBinding ActualWidth}" 
                                          MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <Border x:Name="DropDownBorder" 
                                                Background="White" 
                                                BorderThickness="1" 
                                                BorderBrush="#CED4DA"
                                                CornerRadius="6">
                                            <ScrollViewer Margin="4,6,4,6" 
                                                          SnapsToDevicePixels="True">
                                                <StackPanel IsItemsHost="True" 
                                                            KeyboardNavigation.DirectionalNavigation="Contained"/>
                                            </ScrollViewer>
                                        </Border>
                                    </Grid>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#DC3545"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#DC3545"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Add Button Style -->
        <Style x:Key="AddButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C82333"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#A71E2A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Delete Button Style -->
        <Style x:Key="DeleteButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C82333"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#A71E2A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="30"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="15" ShadowDepth="5" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid Style -->
        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F8F9FA"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E9ECEF"/>
            <Setter Property="VerticalGridLinesBrush" Value="#E9ECEF"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>

        <!-- DataGrid Header Style -->
        <Style x:Key="DataGridHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
        </Style>

        <!-- DataGrid Row Style -->
        <Style x:Key="DataGridRowStyle" TargetType="DataGridRow">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="13"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E9ECEF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#DC3545"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid Cell Style -->
        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,5"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <!-- Main Content Card -->
        <Border Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <TextBlock Grid.Row="0" 
                           Text="Calls Management" 
                           Style="{StaticResource HeaderStyle}"/>

                <!-- Controls Panel -->
                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Sort Controls -->
                    <StackPanel Grid.Column="0" 
                                Orientation="Horizontal" 
                                HorizontalAlignment="Left" 
                                VerticalAlignment="Center">
                        <Label Content="Sort by Field:" 
                               Style="{StaticResource FilterLabelStyle}"/>
                        <ComboBox SelectedValue="{Binding Path=CallFieldsFilter, Mode=TwoWay}" 
                                  ItemsSource="{Binding Source={StaticResource CallFieldsCollectionKey}}" 
                                  SelectionChanged="comboBoxCallList_SelectionChanged" 
                                  Style="{StaticResource FilterComboBoxStyle}"
                                  Width="150"/>
                    </StackPanel>

                    <!-- Filter Controls -->
                    <StackPanel Grid.Column="1" 
                                Orientation="Horizontal" 
                                HorizontalAlignment="Center" 
                                VerticalAlignment="Center">
                        <Label Content="Filter by Call Type:" 
                               Style="{StaticResource FilterLabelStyle}"/>
                        <ComboBox SelectedValue="{Binding SelectedTypeOfCallFilter, Mode=TwoWay}" 
                                  ItemsSource="{Binding Source={StaticResource TypeOfCallCollectionKey}}" 
                                  SelectionChanged="comboBoxCallList_SelectionChanged" 
                                  Style="{StaticResource FilterComboBoxStyle}"
                                  Width="150"/>
                    </StackPanel>

                    <!-- Add Button -->
                    <Button Grid.Column="2"
                            Content="+ Add Call"
                            Click="AddButton_Click" 
                            Style="{StaticResource AddButtonStyle}"
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Center"/>
                </Grid>

                <!-- DataGrid -->
                <DataGrid Grid.Row="2"
                          ItemsSource="{Binding Path=CallList}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectedItem="{Binding SelectedCall}"
                          MouseDoubleClick="lsvCallList_MouseDoubleClick"
                          Style="{StaticResource DataGridStyle}"
                          ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}"
                          RowStyle="{StaticResource DataGridRowStyle}"
                          CellStyle="{StaticResource DataGridCellStyle}">
                    <DataGrid.Columns>
                        <!-- CallId -->
                        <DataGridTextColumn Binding="{Binding CallId}" 
                                            Header="ID" 
                                            Width="80"/>

                        <!-- Type Of Call -->
                        <DataGridTextColumn Binding="{Binding TypeOfCall}" 
                                            Header="Type Of Call" 
                                            Width="140"/>

                        <!-- Opening Time -->
                        <DataGridTextColumn Binding="{Binding OpeningTime}" 
                                            Header="Opening Time" 
                                            Width="150"/>

                        <!-- Time Left -->
                        <DataGridTextColumn Binding="{Binding TimeLeft}" 
                                            Header="Time Left" 
                                            Width="100"/>

                        <!-- Last Volunteer Name -->
                        <DataGridTextColumn Binding="{Binding LastVolunteerName}" 
                                            Header="Last Volunteer" 
                                            Width="150"/>

                        <!-- Treatment Duration -->
                        <DataGridTextColumn Binding="{Binding TreatmentDuration}" 
                                            Header="Treatment Duration" 
                                            Width="130"/>

                        <!-- Status -->
                        <DataGridTextColumn Binding="{Binding Status}" 
                                            Header="Status" 
                                            Width="100"/>

                        <!-- Assignments Sum -->
                        <DataGridTextColumn Binding="{Binding AssignmentsSum}" 
                                            Header="Assignments" 
                                            Width="100"/>

                        <!-- Delete button -->
                        <DataGridTemplateColumn Header="Actions" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Click="DeleteButton_Click" 
                                            Tag="{Binding CallId}" 
                                            Content="✕"
                                            Style="{StaticResource DeleteButtonStyle}"
                                            ToolTip="Delete Call"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>