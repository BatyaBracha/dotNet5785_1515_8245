<!--<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Icon="icon.ico"
        Title="MainWindow" Height="390" Width="700"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    --><!-- Define Styles --><!--
    <Window.Resources>
        --><!-- Label Style --><!--
        <Style x:Key="GridLabelStyle" TargetType="Label">
            <Setter Property="Foreground" Value="#2E3A59"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            --><!--<Setter Property="Margin" Value="5"/>--><!--
        </Style>

        --><!-- TextBox Style --><!--
        <Style x:Key="GridTextBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Background" Value="#F7F9FC"/>
            <Setter Property="BorderBrush" Value="Green"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="90"/>
            --><!--<Setter Property="Margin" Value="5"/>-->
            <!--<Setter Property="Padding" Value="4,2"/>--><!--
        </Style>

        --><!-- Button Style --><!--
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="DarkGreen"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            --><!--<Setter Property="Padding" Value="10,5"/>-->
            <!--<Setter Property="Margin" Value="5"/>--><!--
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="DarkGreen"/>
                    <Setter Property="Foreground" Value="LightGray"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="500*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        --><!-- Internal, Upper Grid --><!--
        <Grid Grid.Row="0" Margin="0,0,0,10" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="73*"/>
                <ColumnDefinition Width="67*"/>
            </Grid.ColumnDefinitions>

            --><!-- Sub-grid for Label, TextBox, and Button --><!--
            <Grid Grid.Column="0" Margin="10,72,330,138" Grid.ColumnSpan="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.2*"/>
                </Grid.ColumnDefinitions>

                <Label Margin="0,89,0,0" VerticalAlignment="Top" Content="Risk range:" FontSize="20" Style="{StaticResource GridLabelStyle}" Height="37" Width="108"/>
                <TextBox Margin="4,89,19,0" TextWrapping="Wrap" Text="{Binding RiskRange}" VerticalAlignment="Top" Style="{StaticResource GridTextBoxStyle}" Grid.Column="1"/>
                <Button Grid.Column="2" Content="Update" HorizontalAlignment="Stretch" Height="40"  Click="btnSetRiskRange_Click" Style="{StaticResource PrimaryButtonStyle}" VerticalAlignment="Top" Width="110" Margin="10,88,15,0" RenderTransformOrigin="0.455,0.201"/>
                <Label  VerticalAlignment="Top" Content="Interval:" FontSize="20" Style="{StaticResource GridLabelStyle}" Height="37" Width="108" Margin="0,16,0,0"/>
                <TextBox IsReadOnly="{Binding IsSimulatorRunning}" TextWrapping="Wrap" Text="{Binding Interval}" VerticalAlignment="Top" Style="{StaticResource GridTextBoxStyle}" Margin="4,14,19,0" Grid.Column="1" />
                <Button Grid.Column="2" Content="{Binding SimulatorButtonText}"  HorizontalAlignment="Stretch" Height="40"  Click="btnSimulator_Click" Style="{StaticResource PrimaryButtonStyle}" VerticalAlignment="Top" Width="110" Margin="10,14,15,0" />

            </Grid>

            --><!-- Clock and other buttons --><!--
            <Label Margin="0,40,0,0" VerticalAlignment="Top" Width="232" Content="{Binding CurrentTime}" FontWeight="Bold" FontSize="20" Style="{StaticResource GridLabelStyle}" HorizontalAlignment="Center" Grid.Column="1" Height="40"/>
            <Button  IsEnabled="{Binding IsButtonsEnabled}" Grid.Column="1" Content="Add one minute" Margin="0,85,0,0" VerticalAlignment="Top" Width="312" Click="btnAddOneMinute_Click" Style="{StaticResource PrimaryButtonStyle}" Height="40"/>
            <Button IsEnabled="{Binding IsButtonsEnabled}" Grid.Column="1" Content="Add one hour" Width="312" Click="btnAddOneHour_Click" Style="{StaticResource PrimaryButtonStyle}" Margin="0,136,0,0" VerticalAlignment="Top" Height="40"/>
            <Button IsEnabled="{Binding IsButtonsEnabled}" Grid.Column="1" Content="Add one day" Width="312" RenderTransformOrigin="0.502,0.253" Click="btnAddOneDay_Click" Style="{StaticResource PrimaryButtonStyle}" Margin="0,186,0,0" VerticalAlignment="Top" Height="40"/>
            <Button IsEnabled="{Binding IsButtonsEnabled}" Grid.Column="1" Content="Add one month" Margin="0,238,0,0" VerticalAlignment="Top" Width="312" Click="btnAddOneMonth_Click" Style="{StaticResource PrimaryButtonStyle}" Height="40"/>
            <Button IsEnabled="{Binding IsButtonsEnabled}" Grid.Column="1" Content="Add one year" Margin="0,289,0,0" VerticalAlignment="Top" Width="312" Click="btnAddOneYear_Click" Style="{StaticResource PrimaryButtonStyle}" Height="40"/>
            <TextBlock HorizontalAlignment="Left" Margin="89,0,0,0" TextWrapping="Wrap" Text="welcome" VerticalAlignment="Top" Height="65" Width="206" FontSize="50"/>
            --><!--<Image HorizontalAlignment="Left" Height="100" Margin="20,48,0,0" VerticalAlignment="Top" Width="332" Source="./Images/welcome.png"/>--><!--
        </Grid>

        --><!-- Lower Buttons --><!--
        <Button IsEnabled="{Binding IsButtonsEnabled}" Content="Reset DB" Width="158" Margin="21,211,0,0" Grid.Row="1" Click="btnResetDB_Click" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left" VerticalAlignment="Top" Height="40"/>
        <Button Content="Handle Volunteers" Width="158" Margin="196,211,0,0" Grid.Row="1" Click="btnVolunteerList_Click" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left" VerticalAlignment="Top" Height="40"/>
        <Button IsEnabled="{Binding IsButtonsEnabled}" Content="Init DB" Width="158" Margin="21,262,0,0" Grid.Row="1" Click="btnInitializeDB_Click" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left" VerticalAlignment="Top" Height="40"/>
        <Button Content="Handle Calls" Width="158" Margin="196,262,0,0" Grid.Row="1" Click="btnCallList_Click" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left" VerticalAlignment="Top" Height="40"/>
    </Grid>
</Window>-->
<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Icon="icon.ico"
        Title="We Save Lives - Main Dashboard" Height="590" Width="800"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Background="#F8F9FA">

    <!-- Define Styles -->
    <Window.Resources>
        <!-- Header Style -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#DC3545"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,20,0,10"/>
        </Style>

        <!-- Subtitle Style -->
        <Style x:Key="SubtitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#6C757D"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,30"/>
        </Style>

        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="8" ShadowDepth="2" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Label Style -->
        <Style x:Key="GridLabelStyle" TargetType="Label">
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="GridTextBoxStyle" TargetType="TextBox">
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Focusable="false" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#DC3545"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C82333"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#A71E2A"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#6C757D"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CED4DA"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8F9FA"/>
                    <Setter Property="BorderBrush" Value="#DC3545"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Time Display Style -->
        <Style x:Key="TimeDisplayStyle" TargetType="Label">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Label">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="WE SAVE LIVES" Style="{StaticResource HeaderStyle}"/>
            <TextBlock Text="Emergency Response Management System" Style="{StaticResource SubtitleStyle}"/>
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Column - Control Panel -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="10,10,10,10">
                <StackPanel>
                    <TextBlock Text="Control Panel" FontSize="18" FontWeight="Bold" Foreground="#495057" Margin="0,0,0,20"/>

                    <!-- Risk Range Section -->
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Label Grid.Column="0" Grid.Row="0" Content="Risk Range:" Style="{StaticResource GridLabelStyle}"/>
                        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding RiskRange}" Style="{StaticResource GridTextBoxStyle}" Margin="0,0,10,0"/>
                        <Button Grid.Column="2" Grid.Row="0" Content="Update" Click="btnSetRiskRange_Click" Style="{StaticResource PrimaryButtonStyle}" Width="80"/>
                    </Grid>

                    <!-- Interval Section -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Label Grid.Column="0" Content="Interval:" Style="{StaticResource GridLabelStyle}"/>
                        <TextBox Grid.Column="1" IsReadOnly="{Binding IsSimulatorRunning}" Text="{Binding Interval}" Style="{StaticResource GridTextBoxStyle}" Margin="0,0,10,0"/>
                        <Button Grid.Column="2" Content="{Binding SimulatorButtonText}" Click="btnSimulator_Click" Style="{StaticResource PrimaryButtonStyle}" Width="80"/>
                    </Grid>

                    <!-- Database Controls -->
                    <TextBlock Text="Database Controls" FontSize="16" FontWeight="SemiBold" Foreground="#495057" Margin="0,10,0,15"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Button Grid.Column="0" Grid.Row="0" IsEnabled="{Binding IsButtonsEnabled}" Content="Initialize DB" Click="btnInitializeDB_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                        <Button Grid.Column="1" Grid.Row="0" IsEnabled="{Binding IsButtonsEnabled}" Content="Reset DB" Click="btnResetDB_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                        <Button Grid.Column="0" Grid.Row="1" Content="Handle Volunteers" Click="btnVolunteerList_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                        <Button Grid.Column="1" Grid.Row="1" Content="Handle Calls" Click="btnCallList_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Right Column - Time Controls -->
            <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="10,10,10,10">
                <StackPanel>
                    <TextBlock Text="Time Management" FontSize="18" FontWeight="Bold" Foreground="#495057" Margin="0,0,0,20"/>

                    <!-- Current Time Display -->
                    <Label Content="{Binding CurrentTime}" Style="{StaticResource TimeDisplayStyle}" Margin="0,0,0,20"/>

                    <!-- Time Control Buttons -->
                    <StackPanel>
                        <Button IsEnabled="{Binding IsButtonsEnabled}" Content="Add One Minute" Click="btnAddOneMinute_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                        <Button IsEnabled="{Binding IsButtonsEnabled}" Content="Add One Hour" Click="btnAddOneHour_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                        <Button IsEnabled="{Binding IsButtonsEnabled}" Content="Add One Day" Click="btnAddOneDay_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                        <Button IsEnabled="{Binding IsButtonsEnabled}" Content="Add One Month" Click="btnAddOneMonth_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                        <Button IsEnabled="{Binding IsButtonsEnabled}" Content="Add One Year" Click="btnAddOneYear_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
